# pages.yml
content:
  - name: portfolio
    label: Portfolio Pieces
    path: src/content/portfolio
    filename: "{year}-{month}-{day}-{fields.title}.md"
    type: collection
    fields:
      - name: title
        type: string
      - name: description
        type: string
      - name: image
        type: string
      - name: publishDate
        type: date
